# S3 工作记录

## 归一化选择

- S1 六项能力保持原 ID，不按课程章节重命名。
- `EVID-201`–`216` 先归一化到真实任务，再形成 12 个学习单元和 12 个节点。
- provenance/claim-audit 作为横切能力只保留 `N-011`,`N-012`，通过 supporting mapping 复用。

## 结构推断边界

能力分组、单元粒度和软先修属于 `model_hypothesis` 的结构编排；节点事实、硬边理由和评估边界均可回溯 S2。没有把结构推断重新标成外部事实。

## 条件确认判断

当前目标、六项能力和 S2 回答只导向一个稳定主干；`N-008`,`N-010` 的商业/退出阈值分歧已在节点内部保留适用条件，不形成会实质改变诊断题的并列图谱。因此确认状态为 `conditional / acknowledged`，未冒充用户确认。
